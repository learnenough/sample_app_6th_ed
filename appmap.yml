name: sample_app_6th_ed
packages:
  - path: app
  - path: lib
language: ruby
appmap_dir: tmp/appmap
test_recording:
  test_commands:
    - env:
        APPMAP: 'true'
        DISABLE_SPRING: 'true'
      command: bundle exec rails test
